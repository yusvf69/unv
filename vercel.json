{
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/universe run build && rm -rf public && cp -R artifacts/universe/dist/public public",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
