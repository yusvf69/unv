{
  "framework": null,
  "buildCommand": "cd artifacts/universe && pnpm vite build && cd ../.. && rm -rf public && cp -R artifacts/universe/dist/public public",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
